config {
    type: "table",
    schema: "silver_layer",
    name: "customers"
}

SELECT
DISTINCT
  customer_id,
  customer_city,
  customer_state
FROM ${ref({schema: "bronze_layer", name: "customers"})}
WHERE customer_id is not null